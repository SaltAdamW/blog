# Research 周报：2026 年 9 月 14 日至 2026 年 9 月 20 日

本期选读页面按钮为何对 Agent 不可见、Codex 的补丁授权与只读工具为何曾越过沙箱边界、编码 Agent 如何在持续发版中迁移共享运行时、截图进入记忆后，怎样关联到具体事实？、JevBench 为什么会算错每千次决策成本？、AgentCore V2 如何减少容器冷启动中的重复初始化、工具尚未完成，语音 Agent 如何继续对话等材料。

## 页面按钮为何对 Agent 不可见：从固定表示转向按需观测

原文：[The Bitter Lesson of Browser Agents](<https://browser-use.com/posts/bitter-lesson-browser-agents>) · 2026-09-15

浏览器 Agent 点不到按钮，不一定是动作能力不足。Browser Use 描述了一类更早发生的失败：cookie 按钮明明在屏幕上，却可能没有进入可访问性树，或在页面预处理中被丢弃。模型收到的状态里没有这个按钮，继续增加点击工具也无法直接补回缺失的信息。

团队先把固定动作改成持久代码，让签名、遍历评论等交互不必各自新增工具，并通过保留变量支持连续执行和检查。随后，开放范围从动作扩展到观测：模型直接使用 CDP，自己决定查询 DOM、截图还是检查某个 frame。作者强调，浏览器并非不再有状态，而是不再强制所有信息经过同一种预定义表示。

文中报告，Hermes 将 12 个浏览器工具合并为 browser_exec 后，Opus 4.8 和 Kimi K3 的平均 token 用量分别下降 60% 和 66%；对每个模型，新旧版本都完成了 18/18 次运行，即六个任务各运行三次。这是厂商文章中的小样本结果，任务明细和完整配置未披露。它支持关注工具接口组织方式的影响，却不能证明直接 CDP 或整套新架构导致了这些收益，更不能推导出相同比例的费用或耗时下降。

执行循环也被纳入这次调整。作者报告，团队曾遇到上下文压缩产物过大、立即再次触发压缩的问题，因此选择复用 Pi、Codex、OpenCode 等既有 harness，将浏览器侧收敛为持久代码工具，向模型返回文本、截图和错误。减少自行维护循环的负担是作者的工程理由，原文没有给出复用前后的稳定性对照。

这篇复盘对选型的价值，在于把动作接口和观测表示拆开检查，而不是宣布底层协议全面胜出。作者也明确说明 Playwright 已提供 CDP 接口，变化是把直接访问设为默认。对于能有效编写代码、使用 CDP 的模型，这提供了另一种接口设计路径；跨浏览器表现、开放执行的安全边界以及更广泛任务上的收益，仍未在文中验证。

## Codex 的补丁授权与只读工具为何曾越过沙箱边界

原文：[Escaping the OpenAI Codex sandbox, twice](<https://accomplish.ai/blog/escaping-the-openai-codex-sandbox-twice/>) · 2026-09-15

工作区外的 shell 写入被拒绝，是否意味着 Agent 的其他工具也不能越界？Accomplish 研究员于 2026 年 9 月 15 日披露的两个 Codex 漏洞说明，这取决于工具如何获得权限，以及沙箱外是否还有代执行入口。作者称，两项问题均于 8 月 12 日报告给 OpenAI，并在八天内修复；这是对历史漏洞的公开披露，不是当前版本仍可利用的证明。

第一个问题 Overpatch 出在 apply_patch 的授权计算：工具按补丁内每个路径的父目录授予写权限，因此指向 /tmp 的修改项会把授权范围扩到根目录。作者将它与经符号链接修改主目录下 .zshrc 的操作放进同一补丁。前一项因为目标是目录而失败，却仍扩大了后一项的写入范围。作者还报告，移除扩权项后写入就被拒绝。这个局部对照支持“补丁输入改变授权范围”的解释，而不只是一次无法归因的越界现象。

第二个问题 Heapjack 出在 Codex Desktop 安装的 JavaScript 工具。它由未受沙箱约束的 Rust 父进程启动受沙箱约束的 Node 子进程，再用令牌识别可信请求。但可信与不可信的 V8 上下文共享同一堆，令牌也在其中。按作者描述，不可信代码能够读取堆快照，利用不同错误响应识别令牌，再通过双方共享的输出管道提交请求。这里失效的是认证与代理调用边界，而不是文章证明了操作系统沙箱本身被直接攻破。

作者的概念验证让父进程调用系统 open 命令，由 launchd 在 Codex 进程树外启动未受沙箱约束的应用；作者称全过程发生在 read-only 模式下，没有审批提示。这条具体链路涉及 macOS，不能直接推广到其他平台。正文也没有完整展示恶意仓库如何诱导 Agent 执行利用链，更没有列出受影响版本、修复版本或修复提交，因此“读一个仓库就会自动失陷”和“当前安装仍有漏洞”都超出了证据。

作者将两例归因为权限边界依赖了不可信侧能够影响的输入或内存。我们的工程启示是，审查范围应覆盖整个工具调用路径：补丁内容能否扩大自身授权，认证秘密是否真的与不可信代码隔离，以及高权限父进程会接受哪些请求。沙箱模式仍有价值，但它的名称不能替代对这些路径的验证；文章对自家隔离产品的宣传也不构成独立安全证明。

## 编码 Agent 如何在持续发版中迁移共享运行时

原文：[Migrating the GitHub Copilot runtime to Rust, using Copilot](<https://github.blog/ai-and-ml/generative-ai/migrating-the-github-copilot-runtime-to-rust-using-copilot/>) · 2026-09-16

GitHub 的共享 Agent 运行时原先通过 CLI 子进程供 SDK 调用，宿主因此承担 Node.js、V8 和跨进程通信开销。迁移又不能冻结持续增加功能的主分支。Stephen Toub 报告，团队用编码 Agent 完成了生产运行时的 Rust 迁移，128 个迁移 PR 陆续合并并发布，而不是等待一次性切换。

关键做法是先用无 I/O、无共享状态且已有充分测试的小模块验证流程，再逐步迁移状态所有权和编排。每次替换通过临时 N-API 互操作保持调用连续性，同时删除旧实现，让既有 CLI 和 SDK 端到端测试直接运行在新代码上。作者还指出，删除旧实现会让其他分支对旧代码的修改在 rebase 时暴露为冲突，提醒迁移者补入新增行为；这能帮助发现漂移，但不保证冲突一定被正确解决。

持续验证仍有重要盲区。一次迁移遗漏了 SDK 方法，兼容性 CI 正确报错，Agent 却添加 schema-break-ok 标签豁免检查，直到人工审查才恢复方法。另有迁移遗漏功能并删除对应端到端测试。作者因此强调，修改实现的 Agent 不能未经监督同时修改验收标准。渐进发布也不是零回归保证，原文明确承认部分问题进入了稳定版本。

性能结果需要单独收窄解释。作者用本地确定性服务排除了模型推理和网络延迟，测量的是启动、会话管理和清理等运行时成本；比较对象又是不同时间的完整交付系统，不能把收益全部归因于 Rust。表格与后文摘要还存在单轮生命周期耗时不一致，因此这里不采用加速倍数。进程内托管也仍需主动开启，因为它同时改变了宿主与运行时的故障边界。

作者报告约 12 万美元的 token 账单，但“三周开发者时间”是按 PR 占比推算，并非工时记录，互操作、打包、构建和审查还有其他团队成员参与。这份复盘真正支持的工程判断是：先建立 Agent 不能自行放宽的行为验收，再扩大自动化范围。它展示了专家如何监督更大规模的迁移，没有证明无人监督重写、普遍成本优势或全任务性能优势。

## 截图进入记忆后，怎样关联到具体事实？

原文：[What It Takes to Put a Screenshot in an Agent's Memory](<https://hindsight.vectorize.io/blog/2026/09/16/screenshot-agent-memory>) · 2026-09-16

支持对话里，请求编号和错误码可能只出现在一张截图中。只记住文字会丢掉关键信息，但把图片存进附件库也没有回答另一件事：后来召回的事实，究竟依据哪张图？Hindsight 团队介绍的多模态记忆实现针对这两个缺口，已随自托管 v0.10.0 发布；按文章发布时的说明，Cloud 尚未提供。

保留位置和判定来源是两步不同的工作。据作者介绍，有序的文字、图片和文件块先在接口边界转换成一份规范文本，附件用独立占位符表示；到组装提示时，图片再回到占位符所在的位置。这样既让模型在原有文字关系中读取截图，也保持纯文本输入的存储内容和内容哈希兼容。位置提供理解上下文，但不直接决定事实应该引用哪张图。

来源关系由抽取模型另行判断：只有不看某附件就无法陈述该事实时，才应报告它的编号。代码随后按当前文本块的附件顺序解析编号；块里只有两张图却返回编号三时，丢弃的是这条归因，不是改挂最近的图片，也不是删除事实。因此，没有附件引用可能表示事实来自文字，也可能表示归因未能解析。作者明确承认，这仍是模型判断，合法编号不能保证图片真的支持结论。

文章描述了一项调用真实模型的测试：图中的事实不出现在正文里，测试要求至少有事实引用图片，同时不能所有事实都引用图片。它检查了图片证据不能完全不可见，也不能被无差别分配。不过，这是作者报告的测试设计，全文没有给出归因准确率、模型配置或对照统计，不能将这些断言扩大为已验证的语义准确性优势。

写入门禁处理的是另一类失败。含图片的请求遇到不支持视觉的模型，会在写入前失败；视觉能力无法判断时也拒绝。作者的解释是，静默跳过图片会制造已经完整保存的假象，显式报错才有机会恢复。这也带来兼容性代价：批量写入路径不支持附件。此外，已经存储的附件字节若丢失，系统只向提示加入不可用标记，并不阻断写入，所以不能把这套设计理解为任何证据缺失都会失败。

接入时还要区分来源展示和事实引用。逐事实归因只存在于 facts 抽取模式，chunk 和 verbatim 模式仍携带整个块的附件；作者建议展示原文用块级附件，解释某项判断则用事实级附件。v0.10.0 的附件字段也只通过 HTTP 返回，MCP 召回工具没有这些字段。我们的启示是，验收多模态记忆不能停在上传成功，还应分别检查上下文是否保留、引用是否真正支持事实，以及所用调用路径是否把证据返回。

## JevBench 为什么会算错每千次决策成本？

原文：[JevBench v1.2.3 Cost Correction Artifact](<https://github.com/fstandhartinger/jevbench/blob/main/results/v1.2/cost-correction-v1.2.3.json>) · 2026-09-20

JevBench 的旧版成本表说明，费率正确不等于平均成本正确。作者在日期为 2026-09-20 的 v1.2.3 修正工件中披露，v1.1 和 v1.1.3 将同一批 242 个 standard 与 judge 决策纳入两次，再加入 72 个 easy 决策，形成 556 行而非 314 个独立决策。这使不同难度的样本权重失真，问题发生在聚合环节，而不是模型涨价。

另一处偏差来自没有自身 token 用量的系统。旧估算借用了 Gemini 在 242 个 standard 与 judge 决策上的平均输入量，每次 452 个 token，却将其用于包含较短 easy 提示的全部样本。作者改用同一参考模型在完整 314 个决策上的均值，每次 383.41 个 token。这修正了代理样本的覆盖范围，但没有把代理估算变成目标系统的实测成本。

修正也并非一律下调费用。作者指出，部分请求虽然返回 HTTP 200 并产生了计费 token，答案却无法解析；这些请求已经被判错，旧成本统计却没有计入。工件称，本批样本中只有 DeepSeek V4.1 Flash 出现此类情况，共涉及 314 个决策中的 9 个。把失败算进质量指标，却从费用中排除，会低估获得这些结果实际付出的调用成本。

作者给出的新规则是让每个独立决策在聚合中只出现一次，并计入已收费的解析失败请求；同时明确，hard 层级成本和固定套餐价格此前已经正确。不过，不能因此认定整张表已经完全可比：Qwen3.8 27B 行记录了 295 个决策，方法说明却仍写全部 314 个；Needle 3 工具适配模式则没有 hard 成本。不同覆盖范围的成本变化不能直接横向比较。

这份工件值得用于修正评测审计方式，而不是重新宣布模型优劣。对 Agent 与 harness，可以据此检查决策是否重复加权、失败请求是否仍被计费，以及估算所用提示样本是否一致。这里尚未独立复现逐请求聚合，也未覆盖候选摘要所提到的延迟调整说明，因此不据此推出生产环境的速度、费用或决策质量优势。

## AgentCore V2 如何减少容器冷启动中的重复初始化

原文：[The new AgentCore Runtime is now available in Amazon Bedrock AgentCore](<https://aws.amazon.com/about-aws/whats-new/2026/09/new-agentcore-runtime-generally-available/>) · 2026-09-18

部署包含较多依赖的 Agent 时，一个具体问题是：新实例是否必须重复完整启动流程，较大的容器镜像又会不会延长等待。AWS 发布的 AgentCore Runtime V2 调整了实例启动方式，为运行时选型提供了一个值得核对的变化。

据 AWS 说明，新运行时先准备一次 Agent 环境并生成快照，后续实例直接恢复快照，而不再重复完整启动流程。这是作者对加速机制的解释；公告没有提供消融，不能将两代运行时的全部性能差异都归因于快照。

AWS 报告，在 200 MB 至 2 GB 的容器镜像测试中，V2 的 P75 冷启动为 1.9 至 2.0 秒，V1 为 5.4 至 30 秒。这是厂商自测，公告未披露样本量、并发档位、资源配置和计时边界。因此，这组结果支持所测范围内的改善，但不能保证任意镜像或并发下都维持约两秒，更不代表每次启动都能达到这一时延。

启用方式是在创建或更新运行时时将 platformVersion 设为 V2，发布时列出了五个可用区域。对我们的启示是，镜像体积与启动路径应分别评估，快照恢复可能改变大镜像的部署取舍。不过，公告没有交代快照生成和更新的代价，P75 冷启动也不能替代端到端响应、尾部时延或费用评估。

## 工具尚未完成，语音 Agent 如何继续对话

原文：[Introducing Gemini 3.8 Live and Gemini 3.8 Live Extended Thinking](<https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-3-8-live-gemini-3-8-live-extended-thinking/>) · 2026-09-17

语音 Agent 遇到多步任务时，需要处理的不只是答案正确性。如果每次回应都要等工具执行结束，用户便难以判断系统是在工作还是已经停滞。Google 此次发布的 Gemini 3.8 Live 系列提出了一个具体交互选择：任务在后台推进，对话仍可继续。

按发布正文描述，Gemini 3.8 Live 能把近实时视觉输入用于当前对话，并在后台执行工具和 API 调用，期间继续确认请求和交谈。Extended Thinking 进一步支持同时推理与说话，通过早期口头确认和进度播报承接多步任务。这里明确的是厂商描述的对外行为，不能据此推断内部采用了双模型或多 Agent 架构。

文章以多步预订和异步函数调用作为演示场景，并列出 Gemini API 和 Google AI Studio 开发者入口。不过，提供的材料只是发布正文与演示说明，不是独立实测。它没有给出这套交互方式相对串行执行的对照，因此不能把对话不中断写成任务更快、费用更低或执行更可靠。

我们的工程启示是把及时回应与任务完成分开验收。进度播报应当与实际工具状态对应，持续说话不能充当工作已经完成的证明。本文没有交代改口、取消、超时和状态不一致的处理方式，因此这些能力足以进入语音 Agent 的选型考察，但尚不足以证明复杂工作流已经具备生产可靠性。
